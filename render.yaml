services:
  - type: web
    name: tdm-franchise-onboarding
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
